# Default rc.local for void; add your custom commands here.
#
# This is run by runit in stage 2 before the services are executed
# (see /etc/runit/2).
command -v bluetooth >/dev/null 2>/dev/null &&
	bluetooth off

chvt 2
# vim: ft=sh
